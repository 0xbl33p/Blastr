# ── secrets ──
.env
.env.*
!.env.example

# ── private docs (kept local, not for the public repo) ──
PROPOSAL.md
DEPLOY.md

# ── runtime data (legacy local state, not used in prod) ──
data/

# ── build output ──
dist/
*.tsbuildinfo

# ── dependencies ──
node_modules/

# ── logs ──
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# ── editor / IDE / OS ──
.DS_Store
Thumbs.db
.idea/
.vscode/
.claude/
*.swp
*.swo
